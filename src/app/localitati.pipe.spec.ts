import { LocalitatiPipe } from './localitati.pipe';

describe('LocalitatiPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalitatiPipe();
    expect(pipe).toBeTruthy();
  });
});
